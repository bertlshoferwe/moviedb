let express,cors,mongoose;_37d‍.w("express",[["default",["express"],function(v){express=v}]]);_37d‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_37d‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



// obtain variables from dotenv file
require('dotenv').config();

// express server variables
const app = express();
const port = process.env.PORT || 5000;

// setup express middleware
app.use( cors() );
app.use( express.json() );

const uri = process.env.ATLAS_URI;
mongoose.connect(uri, {useNewUrlParser: true, useCreateIndex: true}
);

const connection = mongoose.connection;
connection.once('open', () => {
    console.log('MongoDB database connecton established successfully')
})

// listens for server on port and display in console
app.listen( port, () => {
    console.log( `Server is running on port: ${port}` );
});